services:
  documentserver:
    image: onlyoffice/documentserver:latest
    environment:
      - PUID=1000
      - PGID=100
      - JWT_SECRET=${SECRET}
    volumes:
      - /messier/apps/onlyoffice/logs:/var/log/onlyoffice
      - /messier/apps/onlyoffice/data:/var/www/onlyoffice/Data
      - /messier/apps/onlyoffice/lib:/var/lib/onlyoffice
      - /messier/apps/onlyoffice/db:/var/lib/postgresql
      - /messier/apps/onlyoffice/redis:/var/lib/redis
      - /messier/apps/onlyoffice/rabbitmq:/var/lib/rabbitmq
      - /messier/apps/onlyoffice/fonts-custom:/usr/share/fonts/truetype/custom
    labels:
      - "traefik.enable=true"
      - "traefik.docker.allowNonRunning=true"
      - "traefik.http.routers.onlyoffice.rule=Host(`onlyoffice.${DOMAIN}`)"
      - "traefik.http.routers.onlyoffice.entrypoints=https"
      - "traefik.http.routers.onlyoffice.tls.certresolver=${CERTRESOLVER:+${CERTRESOLVER}}"
      - "traefik.http.routers.onlyoffice.service=onlyoffice"
      - "traefik.http.routers.onlyoffice.middlewares=secure-headers@file"
      - "traefik.http.services.onlyoffice.loadbalancer.server.port=80"
    networks:
      - virgo
    restart: unless-stopped

networks:
  virgo:
    external: true
